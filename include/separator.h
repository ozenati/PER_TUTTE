#ifndef SEPARATOR_H
#define SEPARATOR_H

vector<vector<MyNode *> *> *separateMap2Vectors(map<int, MyNode *> *all_nodes);

map<int, MyNode *> *convertGraph2Map(Graph *graph);

void applyVectors2Graph(vector<vector<MyNode *> *> *vectors, Graph * graph);


#endif	/* SEPARATOR_H */
