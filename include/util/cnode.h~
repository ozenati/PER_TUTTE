#ifndef CNODE_H
#define CNODE_H

typedef struct
{
  float x;
  float y;
}position;

typedef struct
{
  position pos;
  int id;
  int set;
  list *neighbor;
}node;

node *create_node(position pos);
int add_neighbour(node *n1, node *n2);
void free_node(node *n);

#endif
